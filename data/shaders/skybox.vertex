#version 410 core
layout(location = 0) in vec3 aPosition;
uniform mat4 uTransform;
out vec3 sTexCoord;

void main() {
    sTexCoord = aPosition;
    gl_Position = uTransform * vec4(aPosition, 1.0);
}
